{
  "homepage": "https://hristogochev.github.io/wirefox_remote_site",
  "name": "wirefox_remote_site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/input.css -o ./src/build.css --watch & serve ./src",
    "build": "./build.sh",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
